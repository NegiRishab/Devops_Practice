#!/bin/bash

# read -p "is this for memore coose M or for cpu choose C: " data2
# read -p "how many processes do you want to see: " number

# if [[ "$data2" == "M" || "$data2" == "m" ]]
# then 
#    ps aux --sort=-%mem | grep -v "^\s*USER" | head -n "$number"
# elif [[ "$data2" == "C" || "$data2" == "c" ]]
# then 
#     ps aux --sort=-%cpu | grep -v "^\s*USER" | head -n "$number"
# else 
#     echo "invalid input"
# fi




# learning from this code is ps aux and  ^ use of this and head -n number to show the few heads outoupt 