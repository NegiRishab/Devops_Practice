# Copy to .env and adjust values for your environment.
# Do not commit .env — it may contain secrets.

# --- Server (EC2 public IP for testing) ---
SERVER_HOST=13.232.43.163

# --- MySQL ---
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=employees_db
MYSQL_PORT=3306

# --- Backend ---
DB_HOST=mysql
DB_PORT=3306
DB_NAME=employees_db
DB_USER=root
DB_PASSWORD=root
SERVER_PORT=8080
BACKEND_PORT=8080

# --- Frontend ---
# Baked into the image at build time (browser must reach this URL)
VITE_API_BASE_URL=http://13.232.43.163:8080/api
FRONTEND_PORT=3000
FRONTEND_CONTAINER_PORT=4173

